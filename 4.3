#include <iostream>
#include <cstdlib>
#include <conio.h>
using namespace std;
void ale(int n, double  a) {
	double suma = 0.00, multi = 1.00, divi = 1.00, ab = -1.00;
	for (int i = 1; i <= n; i++) {
		ab = ab * -1;
			if (i == 1) a = a;
			else {
				a = pow(a, 2);
		}
		suma = suma + (ab * ((2 * i) - 1) * a) / (2 * i);
		
	}
	cout << "El resultado de serie sera : " << suma << endl;
}
int main() {
	int n;
	double a;
	do {
		cout << "Ingrese el valor de N: ";
		cin >> n;
	} while (n > 20 || 0 > n);
	do {
		cout << "Ingrese el valor de a: ";
		cin >> a;
	} while (a > 2 || 0.5 > a);
	ale(n, a);
	return 0;
}
